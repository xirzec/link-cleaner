{
  "name": "link-cleaner",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "A private, browser-only tool for removing tracking parameters from shared links.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "npm run test && npm run check && npm run build",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "allowScripts": {
    "esbuild": true
  }
}
